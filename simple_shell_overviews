#include <stdlib.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 (success)
 */
int main(void)
{
    system("ls");
    system("betty utility.c");
    system("gcc utility.c");
    system("clear");
    system("ls");
    system("gcc utility.c");
    return 0;
}

